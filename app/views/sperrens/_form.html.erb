<%= form_for(@sperren) do |f| %>
  <% if @sperren.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sperren.errors.count, "error") %> prohibited this sperren from being saved:</h2>

      <ul>
      <% @sperren.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :startDate %><br />
    <%= f.date_select :startDate %>
  </div>
  <div class="field">
    <%= f.label :endDate %><br />
    <%= f.date_select :endDate %>
  </div>
  <div class="field">
    <%= f.label :reason %><br />
    <%= f.text_field :reason %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
